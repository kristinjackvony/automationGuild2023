{
  "name": "automationguild23",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --browser chrome",
    "apiTests": "npx cypress run --spec 'cypress/e2e/apiTests' --browser chrome",
    "performanceTests": "npx cypress run --spec 'cypress/e2e/performanceTests' --browser chrome",
    "visualTests": "npx cypress run --spec 'cypress/e2e/visualTests' --browser chrome",
    "securityTests": "npx cypress run --spec 'cypress/e2e/securityTests' --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cypress-audit/lighthouse": "^1.3.1",
    "cypress": "^11.1.0"
  },
  "dependencies": {
    "@applitools/eyes-cypress": "^3.27.8"
  }
}
